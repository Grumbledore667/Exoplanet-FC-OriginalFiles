local NodeState = {
   ---@class ai.NodeState.SUCCESS
   SUCCESS = {"SUCCESS"},
   ---@class ai.NodeState.FAILURE
   FAILURE = {"FAILURE"},
   ---@class ai.NodeState.RUNNING
   RUNNING = {"RUNNING"}
}

return NodeState
